# Optional — only needed for the Recommendation Agent's AI-assisted narrative.
# Without these set, the app runs entirely in rule-based mode (see PROJECT_SPEC.md, Section 7).
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_VERSION=2024-08-01-preview
